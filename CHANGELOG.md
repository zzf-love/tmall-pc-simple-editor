# 更新记录

## 1.6.0 - 2026-09-04

- 新增**通用版**平台：不再局限于淘宝天猫，输出一段自包含的自适应 HTML，
  可直接粘进任意支持 HTML 的网站（自建站、WordPress、Shopify 等）。
  - 图片 `width:100%`，热点用**百分比**定位，整块随容器宽度等比缩放；
    实测 1920 / 1200 / 768 / 375 四种宽度下热点位置分毫不差。
    （不用 `usemap`：它的 `coords` 是死像素，图片一缩放就错位。）
  - 只有内联样式，不需要额外 CSS 或 JS，不含任何平台系统类名。
  - `<img>` 写入原图 `width`/`height`（避免加载抖动）与 `data-ow`/`data-oh`，
    自己导出的代码可以原样「导入代码」读回继续编辑。
  - 显示方式：铺满 = 宽度 100% 跟随容器；居中 = 最宽不超过原图宽。
  - 通用版没有「店招」概念（那是淘宝页头的固定模块），选中后不显示该选项；
    也没有固定模块窗口，因此顶部像素条不显示可视区高亮段。
- 导入支持百分比定位的热点，通用版代码可无损往返。

## 1.5.4 - 2026-09-04

- 修复**预览（以及线上）图片上下出现白边**的问题。根因在导入：定位图层/通栏代码里
  `<div>` 上写的是**容器**宽高，未必等于原图尺寸，而 `importStoreCodeAsync` 只给
  `<img>` 联网取自然尺寸、跳过了背景图容器（`kind: 'bg'`），于是容器高度被当成图片高度
  存了下来。导出时背景是 `no-repeat center center`，容器比图高多少就上下各留白一半——
  用户的例子里容器 769、原图 700，上下各留白 34.5px。现在背景图容器也会联网取自然尺寸，
  并把写在容器空间的热点坐标减去居中留白，还原回原图像素空间。
- 修复画布与导出不一致：`.canvas-image` 原本是 `object-fit: fill`（拉伸铺满），
  而导出是按原始像素居中，导致尺寸对不上时**画布上看不出问题、导出后才露白边**。
  画布改为 `object-fit: none` + `object-position: center`，与导出完全一致，
  尺寸不符会当场显示出来。
- 已有项目若仍是错误尺寸：选中图片点右侧「重新识别」即可按真实原图尺寸修正。

## 1.5.3 - 2026-09-03

- 修复「预览」窗口在内容下方多出一条模块宽白块的问题：预览容器写了
  `min-height:100vh`，内容比屏幕矮时会被撑到整屏高，白底看起来像多输出了一段内容。
  现在容器只包住真实内容，所见即所得。
- 修复**非 1920 宽的图片在「全屏通栏」下热点整体错位**的问题：通栏容器固定 1920 宽、
  背景按 `center center` 绘制，图片比 1920 窄时会居中留白，但热点仍按原图像素坐标输出，
  于是整组热点偏移 (1920−图宽)/2。990 宽的图会偏出模块 465px，线上完全点不到。
  现在热点跟随同样的居中量平移（含店招固定高度带来的纵向裁切），1920 原图不受影响。
- 「模块可视区」引导改到顶部像素条上高亮一段（带起止像素值），不再在画布上压暗和画虚线框——
  原来的遮罩盖在图片上会干扰框选与拖拽。

## 1.5.2 - 2026-09-03

- 画布新增「模块可视区」引导：通栏模式下在 1920 原图上直接标出当前平台模块的实际显示窗口（天猫 990 居中、淘宝 950 居中、基础版 750 偏右 685px，店招按 990/950），窗口外压暗并附标签，切换左上角平台开关时引导线即时移动，效果一目了然。
- 画布工具栏新增「输出目标」胶囊（平台 + 模块宽 + 页面/店招 + 通栏/居中），任何尺寸的图片都能看出当前平台选择；窄屏自动隐藏。
- 居中显示模式不画可视区引导（整图等比缩放到模块宽，没有通栏窗口）。

## 1.5.1 - 2026-09-03

- 修复外部工具（马工/小语言等）导出的代码导入后画布看不到图片的问题：这类 `<img usemap>` 往往不写 width/height，旧逻辑只能兜底成 1920×0 导致图片不可见；导入改为异步联网读取图片自然尺寸后再建图，area 热点坐标按声明宽度与自然宽度的比例正确还原；图片确实加载失败时给出提示，可选中后点右侧「重新识别」补救。
- 解析代码期间按钮显示加载状态并防止重复点击。

## 1.5.0 - 2026-09-03

- 恢复并修正淘宝基础版（190+750 布局）支持：页面模块宽 750，通栏按右栏实际位置以 -685 偏移实现相对屏幕居中（不能套用 -(1920-宽)/2=-585，否则只居中到栏）；基础版店招仍按 950 宽输出。
- 平台、显示方式、项目类型（页面/店招）与店招高度的选择改为随存储槽分别保存：切换/复制/新建页面、导入项目文件时自动恢复对应设置，不再每次回到默认值。
- 修复店招「全屏通栏」不是真通栏、且导出代码无法再导入的问题（店招通栏改为与页面相同的 1920 突破结构，导入新增兼容路径）。
- 修复店招「居中显示」图片被纵向拉伸：图片等比缩放，外层固定高度裁切。
- 修复居中代码回导后热点停留在缩放坐标、再切通栏整体错位的问题（img 写入 data-ow/data-oh，导入时还原回原图像素空间）。
- 修复淘宝 950 下预览窗口宽度仍写死 990 的问题。
- 店招模式只保留一张店招图：添加即替换，界面不再显示多余图片。
- 清理通栏热点的重复内联样式，热点链接补满整块可点区域。
- 存储槽列表显示每个页面的平台与类型标签。

## 1.4.0 - 2026-09-02

- 新增淘宝兼容：平台可选天猫 990 / 淘宝专业智能版 950 / 淘宝基础版 750。
  宽度、通栏居中偏移、外层系统类名、data-title、后台安装提示都随平台变化。
- 新增店招模式：单图 + 固定高度（120 仅招牌 / 150 招牌加自制导航），
  并在 150 时附带隐藏系统导航的 CSS。
- 新增两种代码格式，可自行选择：
  - 定位图层：绝对定位 div + a 覆盖层，支持 1920 通栏。
  - 图片热区：img usemap + map/area，结构最短，与常见现网装修同构。
- 导入代码支持图片热区结构，可读回 area 的 coords 坐标继续编辑。
- 顶部版本徽标改为跟随所选平台，不再固定显示「天猫版」。

## 1.3.1 - 2026-09-02

- 更名为「热区工坊 HotZone Studio（天猫版）」。
- 修复框选热点时，拖拽中的预览框卡在容器左上角、不跟随鼠标的问题
  （矩形的 x/y 未映射为 CSS 的 left/top）。

## 1.3.0 - 2026-09-01

- 顶部新增“新建页面”，可直接创建空白页面。
- 左侧图片栏支持收起并记忆状态。
- 新增多页面存储槽、页面复制与本地自动保存。
- 商品链接输入区按“未填写/已填写”显示红色或绿色状态。
- 位置与尺寸设置默认收起，热点标题合并显示编号。
- 修复天猫 PC 装修对生成代码的识别兼容性。
- 图片统一以 0 px 间隙输出，支持导入代码继续编辑。

